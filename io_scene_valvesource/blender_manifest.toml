schema_version = "1.0.0"

# Basic Add-on Info
id = "pulse_src_ops"
name = "PulseSrcOps"
version = "0.9.98"
tagline = "A forked of BlenderSourceTools, an Importer and exporter for Valve Software's Source Engine Models"
maintainer = "Toppi"
type = "add-on"

# Blender Compatibility
blender_version_min = "4.5.0"

# Licensing
license = [
    "SPDX:GPL-2.0-or-later"
]

# Permissions
permissions = ["files"]

# Optional Metadata
tags = [
    "Import-Export",
    "Source Engine",
]